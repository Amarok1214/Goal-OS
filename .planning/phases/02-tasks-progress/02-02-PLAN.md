---
phase: 02-tasks-progress
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified: []
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04]
user_setup: []

must_haves:
  truths:
    - "User can add task to a goal"
    - "User can toggle task completion"
    - "User can edit task title and due date"
    - "User can delete task"
  artifacts:
    - path: "src/components/goals/TaskForm.tsx"
      provides: "Modal form for creating/editing tasks"
    - path: "src/components/goals/TaskItem.tsx"
      provides: "Task row with checkbox, title, due date, actions"
---

<objective>
Create Task UI components: form modal and task item row. Wire up all CRUD operations to taskStore.
</objective>

<execution_context>
@.planning/phases/02-tasks-progress/02-CONTEXT.md

# UI decisions:
- TaskForm: Similar to GoalForm but simpler (title + dueDate)
- TaskItem: Checkbox, title, due date, edit/delete buttons
- Add "Add Task" button on GoalCard to open TaskForm
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

# From requirements:
- TASK-01: Add tasks under goals
- TASK-02: Toggle task completion
- TASK-03: Edit task title and due date
- TASK-04: Delete tasks
</context>

<tasks>

<task type="auto">
  <name>Create TaskForm modal component</name>
  <files>src/components/goals/TaskForm.tsx</files>
  <action>
Create TaskForm:
- Use React Hook Form + Zod (like GoalForm but simpler)
- Fields: title (required), dueDate (optional)
- Handle both create and edit modes via editTask prop
- On submit: call addTask (with goalId) or updateTask from taskStore
- Use Dialog, Input, Button from shadcn/ui
  </action>
  <verify>
npm run build succeeds
  </verify>
  <done>TaskForm modal renders and validates</done>
</task>

<task type="auto">
  <name>Create TaskItem component</name>
  <files>src/components/goals/TaskItem.tsx</files>
  <action>
Create TaskItem:
- Checkbox for completion toggle
- Title (strikethrough when completed)
- Due date (formatted, red if overdue)
- Edit and Delete buttons
- Calls toggleTask, deleteTask from taskStore
- Format: compact row layout
  </action>
  <verify>
TaskItem displays with checkbox, title, due date, actions
  </verify>
  <done>TaskItem with toggle, edit, delete functionality</done>
</task>

<task type="auto">
  <name>Integrate tasks into GoalCard</name>
  <files>src/components/goals/GoalCard.tsx</files>
  <action>
Update GoalCard:
- Add "Add Task" button
- Show list of tasks for this goal (using getTasksByGoal)
- Render TaskItem for each task
- Open TaskForm for add/edit
- Handle task actions (toggle, edit, delete)
  </action>
  <verify>
Tasks appear under goal, can be added/edited/deleted/toggled
  </verify>
  <done>Tasks integrated into GoalCard</done>
</task>

</tasks>

<verification>
Manual test:
1. Click "Add Task" on goal → modal opens → add task → task appears
2. Click checkbox → task marked complete
3. Click edit → modify task → save → updates
4. Click delete → confirm → task removed
</verification>

<success_criteria>
- [ ] User can add task to a goal
- [ ] User can toggle task completion (checkbox)
- [ ] User can edit task title and due date
- [ ] User can delete task with confirmation
- [ ] Tasks persist to localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/02-tasks-progress/02-02-SUMMARY.md`
</output>
