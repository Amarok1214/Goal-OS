---
phase: 02-tasks-progress
plan: 03
type: execute
wave: 1
depends_on: [02-02]
files_modified: []
autonomous: true
requirements: [PROG-01, TODAY-01, TODAY-02]
user_setup: []

must_haves:
  truths:
    - "Goal shows progress bar with completion percentage"
    - "Today view shows tasks due today or active"
    - "Filter tabs: All, Due Today, Active"
  artifacts:
    - path: "src/components/goals/GoalCard.tsx"
      provides: "Goal with progress bar"
    - path: "src/components/goals/TodayView.tsx"
      provides: "Today tasks view with filters"
---

<objective>
Add visual progress tracking to goals and create Today view for daily task management.
</objective>

<execution_context>
@.planning/phases/02-tasks-progress/02-CONTEXT.md

# Progress bar:
- Show on GoalCard: "X of Y tasks complete" + visual bar
- Calculate: (completedTasks / totalTasks) * 100
- Show 0% if no tasks

# Today view:
- New component showing today's tasks
- Filters: All | Due Today | Active
- Group by goal or flat list
- Accessible from navigation
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

# From roadmap:
- PROG-01: Visual progress bar per goal
- TODAY-01: Today view showing tasks due today or active
- TODAY-02: Filter by "due today" or "active today"
</context>

<tasks>

<task type="auto">
  <name>Add progress bar to GoalCard</name>
  <files>src/components/goals/GoalCard.tsx</files>
  <action>
Update GoalCard:
- Calculate completion percentage from tasks
- Show progress bar: filled width based on percentage
- Show text: "X of Y tasks complete"
- Style: green bar, gray background
  </action>
  <verify>
Progress bar shows on goal cards with tasks
  </verify>
  <done>GoalCard shows progress bar</done>
</task>

<task type="auto">
  <name>Create TodayView component</name>
  <files>src/components/goals/TodayView.tsx, src/components/goals/index.ts</files>
  <action>
Create TodayView:
- Subscribe to tasks from taskStore
- Show tasks due today OR active (not completed)
- Filter tabs: All, Due Today, Active
- Group tasks by goal or show flat list
- Empty state if no tasks
- Show goal name, task title, due date, checkbox
  </action>
  <verify>
Today view shows correct filtered tasks
  </verify>
  <done>TodayView with filters working</done>
</task>

<task type="auto">
  <name>Add navigation to App.tsx</name>
  <files>src/App.tsx</files>
  <action>
Update App.tsx:
- Add simple navigation (Goals | Today)
- State for current view
- Show GoalList or TodayView based on selection
- Add header navigation UI
  </action>
  <verify>
Can switch between Goals and Today views
  </verify>
  <done>Navigation between Goals and Today working</done>
</task>

</tasks>

<verification>
Manual test:
1. Add tasks to goals, complete some → progress bar updates
2. Click Today → see due/active tasks
3. Filter tabs filter correctly
4. Navigate back to Goals
</verification>

<success_criteria>
- [ ] Goal shows progress bar with percentage
- [ ] Progress bar updates when tasks are toggled
- [ ] Today view shows tasks due today
- [ ] Today view shows active (incomplete) tasks
- [ ] Filter tabs work correctly
- [ ] Navigation between views works
</success_criteria>

<output>
After completion, create `.planning/phases/02-tasks-progress/02-03-SUMMARY.md`
</output>
