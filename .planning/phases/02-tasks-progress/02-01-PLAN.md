---
phase: 02-tasks-progress
plan: 01
type: execute
wave: 1
depends_on: [01-03]
files_modified: []
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04]
user_setup: []

must_haves:
  truths:
    - "Task type defined with goalId, title, dueDate, completed fields"
    - "TaskStore with CRUD operations"
    - "toggleTask method for marking complete/incomplete"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Task interface and TaskStore type"
    - path: "src/store/taskStore.ts"
      provides: "Zustand store with task operations"
---

<objective>
Define Task types and create Zustand store with all CRUD operations including toggle completion.
</objective>

<execution_context>
@.planning/phases/02-tasks-progress/02-CONTEXT.md

# Task type structure:
- id: string (UUID)
- goalId: string (links to parent Goal)
- title: string
- dueDate: Date | null
- completed: boolean
- createdAt: Date
- updatedAt: Date

# Store operations needed:
- addTask(goalId, taskData)
- updateTask(id, updates)
- deleteTask(id)
- toggleTask(id)
- getTasksByGoal(goalId)
- getTodayTasks()
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

# From roadmap:
- TASK-01: Add tasks under goals
- TASK-02: Toggle task completion
- TASK-03: Edit task title and due date
- TASK-04: Delete tasks
</context>

<tasks>

<task type="auto">
  <name>Add Task types to types/index.ts</name>
  <files>src/types/index.ts</files>
  <action>
Extend types:
- Task interface with id, goalId, title, dueDate, completed, createdAt, updatedAt
- TaskStore interface with tasks array and all CRUD methods
- Export both
  </action>
  <verify>
Types can be imported without errors
  </verify>
  <done>Task and TaskStore types defined</done>
</task>

<task type="auto">
  <name>Create taskStore.ts with all operations</name>
  <files>src/store/taskStore.ts</files>
  <action>
Create Zustand store:
- Import Task types
- Use persist middleware with localStorage (key: 'goal-os-tasks')
- Implement: addTask, updateTask, deleteTask, toggleTask, getTasksByGoal, getTodayTasks
- toggleTask: flips completed boolean and updates updatedAt
- getTodayTasks: returns tasks where dueDate is today OR completed is false
- Use crypto.randomUUID() for id generation
  </action>
  <verify>
npm run build succeeds
  </verify>
  <done>TaskStore with all CRUD and toggle operations</done>
</task>

</tasks>

<verification>
Manual test: Add task, toggle completion, refresh page - data persists
</verification>

<success_criteria>
- [ ] Task type defined with all required fields
- [ ] addTask creates task linked to goalId
- [ ] updateTask modifies existing task
- [ ] deleteTask removes task
- [ ] toggleTask flips completed status
- [ ] getTasksByGoal returns tasks for specific goal
- [ ] getTodayTasks returns today's tasks
- [ ] Data persists to localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/02-tasks-progress/02-01-SUMMARY.md`
</output>
