---
phase: 01-foundation-goals
plan: 03
type: execute
wave: 1
depends_on: [01-02]
files_modified: []
autonomous: true
requirements: [GOAL-01, GOAL-02, GOAL-03, GOAL-04, GOAL-05]
user_setup: []

must_haves:
  truths:
    - "User can create goal with title, description, deadline, status"
    - "User can view all goals in card-based list"
    - "User can edit any goal"
    - "User can delete goal with confirmation"
    - "User can change goal status"
  artifacts:
    - path: "src/components/goals/GoalForm.tsx"
      provides: "Modal form for creating/editing goals"
    - path: "src/components/goals/GoalCard.tsx"
      provides: "Card component showing goal details"
    - path: "src/components/goals/GoalList.tsx"
      provides: "List of all goals with empty state"
    - path: "src/components/goals/index.ts"
      provides: "Exports for goal components"
---

<objective>
Implement Goal CRUD UI: modal form for create/edit, card component for display, list view with empty state, and wire up all CRUD operations to the Zustand store.
</objective>

<execution_context>
@.planning/phases/01-foundation-goals/01-CONTEXT.md

# From context - UI decisions:
- Modal dialog for creating/editing goals
- Card-based layout with status badges
- Fields: title (required), description (optional), deadline (date), status (dropdown)
- Empty state: friendly message + CTA button
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-goals/01-CONTEXT.md
@.planning/research/STACK.md

# Requirements:
- GOAL-01: Create goal with title, description, deadline
- GOAL-02: Set goal status (Active, Paused, Completed, Someday)
- GOAL-03: View list of all goals
- GOAL-04: Edit goal
- GOAL-05: Delete goal
</context>

<tasks>

<task type="auto">
  <name>Create GoalForm modal component</name>
  <files>src/components/goals/GoalForm.tsx</files>
  <action>
Create GoalForm component:
- Use React Hook Form + Zod for validation
- Zod schema: title (min 1, max 100), description (optional, max 500), deadline (optional date), status (default: 'active')
- Use shadcn/ui Dialog, Input, Textarea, Select, Button
- Handle both create (empty form) and edit (pre-filled) modes
- On submit: call addGoal or updateGoal from store
- Show validation errors inline
  </action>
  <verify>
npm run build succeeds, form renders in dialog
  </verify>
  <done>GoalForm modal renders and validates input correctly</done>
</task>

<task type="auto">
  <name>Create GoalCard component</name>
  <files>src/components/goals/GoalCard.tsx</files>
  <action>
Create GoalCard component:
- Display: title, description (truncated), deadline (formatted), status badge
- Status badges: Active (green), Paused (yellow), Completed (blue), Someday (gray)
- Actions: Edit button, Delete button
- Delete shows confirmation dialog before deleting
- Use Card component from shadcn/ui
- Format deadline: "Due: MMM DD, YYYY" or "No deadline"
  </action>
  <verify>
Card displays goal info with correct status badge colors
  </verify>
  <done>GoalCard displays goal with status badge and action buttons</done>
</task>

<task type="auto">
  <name>Create GoalList and wire up CRUD</name>
  <files>src/components/goals/GoalList.tsx, src/components/goals/index.ts, src/App.tsx</files>
  <action>
Create GoalList component:
- Subscribe to goals from Zustand store
- Map over goals and render GoalCard for each
- Empty state: "No goals yet. Create your first goal to get started!" + "Add Goal" button
- "Add Goal" button opens GoalForm in create mode

Wire up in App.tsx:
- Import GoalList component
- Add header with "Goal OS" title and "Add Goal" button
- Render GoalList below header
  </action>
  <verify>
All CRUD operations work: create, read, update, delete
  </verify>
  <done>Complete Goal CRUD UI integrated in App.tsx</done>
</task>

</tasks>

<verification>
Manual verification:
1. Click "Add Goal" → modal opens → fill form → submit → goal appears in list
2. Click edit on card → modal opens with data → change → save → card updates
3. Click delete → confirm → goal removed from list
4. Refresh page → goals persist
</verification>

<success_criteria>
- [ ] User can create a goal with title, description, deadline, status
- [ ] User can view all goals in card-based list
- [ ] User can edit any goal's fields
- [ ] User can delete a goal with confirmation
- [ ] User can change goal status
- [ ] Empty state shows when no goals exist
- [ ] All data persists across page refresh
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-goals/01-03-SUMMARY.md`
</output>
