---
phase: 01-foundation-goals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SETUP-01, SETUP-02]
user_setup: []

must_haves:
  truths:
    - "Vite + React + TypeScript project builds without errors"
    - "Tailwind CSS configured and working"
    - "shadcn/ui components available"
    - "Project structure follows best practices"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
    - path: "src/App.tsx"
      provides: "Root component"
    - path: "tailwind.config.js"
      provides: "Tailwind configuration"
    - path: "src/index.css"
      provides: "Global styles with Tailwind"
---

<objective>
Set up the project foundation: Vite + React + TypeScript with Tailwind CSS and shadcn/ui. This is the scaffolding everything else builds on.
</objective>

<execution_context>
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

# From research - standard 2026 stack:
- React 19 + Vite 6 + TypeScript 5
- Zustand 5 for state
- shadcn/ui + Tailwind 4
- React Router 7, React Hook Form 7 + Zod 3
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-goals/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Initialize Vite + React + TypeScript project</name>
  <files>package.json, vite.config.ts, tsconfig.json, index.html, src/main.tsx, src/App.tsx</files>
  <action>
Initialize a new Vite + React + TypeScript project:
- Run: npm create vite@latest . -- --template react-ts
- Install base dependencies: react, react-dom
- Install dev dependencies: @types/react, @types/react-dom, typescript, vite, @vitejs/plugin-react
- Configure vite.config.ts with React plugin
- Configure tsconfig.json with React types
  </action>
  <verify>
npm run build completes without errors
  </verify>
  <done>Project builds successfully with npm run build</done>
</task>

<task type="auto">
  <name>Configure Tailwind CSS</name>
  <files>tailwind.config.js, postcss.config.js, src/index.css</files>
  <action>
Set up Tailwind CSS:
- Install: npm install -D tailwindcss @tailwindcss/vite
- Add Tailwind to vite.config.ts plugins
- Create tailwind.config.js with content paths
- Create postcss.config.js
- Update src/index.css with @import "tailwindcss";
- Add base styles for body and html
  </action>
  <verify>
npm run build includes Tailwind classes
  </verify>
  <done>Tailwind CSS configured and working</done>
</task>

<task type="auto">
  <name>Set up shadcn/ui and project structure</name>
  <files>src/components/ui/*.ts, src/lib/utils.ts, src/components/goals, src/hooks</files>
  <action>
Set up shadcn/ui foundation:
- Create src/lib/utils.ts with cn() utility (class-variance-authority, clsx, tailwind-merge)
- Install dependencies: npm install class-variance-authority clsx tailwind-merge lucide-react
- Create basic shadcn components: Button, Card, Dialog, Input, Select, Textarea, Label
- Set up src/components/goals/ directory structure
- Create src/components/ui/index.ts export file
  </action>
  <verify>
shadcn components can be imported and used
  </verify>
  <done>shadcn/ui components available and project structure created</done>
</task>

</tasks>

<verification>
Run: npm run build
Expected: Build completes without errors
</verification>

<success_criteria>
- [ ] npm run dev starts development server
- [ ] npm run build completes successfully
- [ ] Tailwind CSS classes work in components
- [ ] shadcn/ui Button component renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-goals/01-01-SUMMARY.md`
</output>
