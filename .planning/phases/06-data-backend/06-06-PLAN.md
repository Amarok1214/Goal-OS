---
id: 06-06
wave: 2
autonomous: true
depends_on: []
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
---

# Plan 06-06: Task Due Dates & Priority

## Objective

Add due date and priority fields to tasks, with visual indicators and sorting options.

## Key Files

- `src/types/index.ts`
- `src/store/taskStore.ts`
- `src/components/goals/TaskForm.tsx`
- `src/components/goals/TaskItem.tsx`
- `src/components/goals/TaskItemCompact.tsx`

## Tasks

### Task 1: Update Task Type Definitions

<files>
- src/types/index.ts
</files>

<action>
In Task interface:
- Add `dueDate: string | null` field (ISO date string)
- Add `priority: 'high' | 'medium' | 'low'` field with default 'medium'
</action>

<verify>
- npm run build succeeds
</verify>

<done>
- Task type includes dueDate
- Task type includes priority
</done>

---

### Task 2: Update Task Store

<files>
- src/store/taskStore.ts
</files>

<action>
- Add dueDate and priority to Task type
- Add actions: setTaskDueDate, setTaskPriority
- Add getTasksSortedByPriority()
- Add getTasksSortedByDueDate()
</action>

<verify>
- npm run build succeeds
- Store actions work
</verify>

<done>
- Tasks can have due dates set
- Tasks can have priorities set
- Sorting functions work
</done>

---

### Task 3: Update Task Form

<files>
- src/components/goals/TaskForm.tsx
</files>

<action>
In TaskForm component:
- Add "Due Date" date picker input
- Add "Priority" segmented control (High/Medium/Low)
- Save both fields when creating/editing task
- High = red, Medium = yellow, Low = green
</action>

<verify>
- npm run build succeeds
- Form renders correctly
</verify>

<done>
- Task form has due date picker
- Task form has priority selector
- Both save correctly
</done>

---

### Task 4: Update Task Display Components

<files>
- src/components/goals/TaskItem.tsx
- src/components/goals/TaskItemCompact.tsx
</files>

<action>
In TaskItem and TaskItemCompact:
- Show priority badge (color-coded)
- Show due date if set
- Style: High=red, Medium=yellow, Low=green
- Show overdue indicator for past due dates
</action>

<verify>
- npm run build succeeds
- Components render correctly
</verify>

<done>
- Tasks show priority badge
- Tasks show due date
- Overdue tasks visually indicated
</done>

---

### Task 5: Add Priority Sorting to Goals

<files>
- src/components/goals/GoalCard.tsx
</files>

<action>
In GoalCard:
- Add sort option: by priority (high first), by due date
- Apply sorting to task lists
- Default: priority sort
</action>

<verify>
- npm run build succeeds
</verify>

<done>
- Tasks sort by priority
- Tasks can sort by due date
</done>
