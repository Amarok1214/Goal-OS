---
id: 06-03
wave: 1
autonomous: true
depends_on: []
requirements:
  - DATA-03
  - DATA-04
  - UX-01
---

# Plan 06-03: Settings + Export/Import

## Objective

Create settings page with export/import functionality - allows users to backup/restore data and customize preferences.

## Key Files

- `src/store/settingsStore.ts`
- `src/components/settings/Settings.tsx`
- `src/utils/export.ts`
- `src/utils/import.ts`
- `src/App.tsx`

## Tasks

### Task 1: Create Settings Store

<files>
- src/store/settingsStore.ts
</files>

<action>
Create `settingsStore.ts` with Zustand:
- Settings interface: pomodoroWork, pomodoroShortBreak, pomodoroLongBreak, notificationsEnabled, soundEnabled
- Persist to localStorage
- Default values: 25, 5, 15, true, true
- Actions: updateSettings, resetSettings
</action>

<verify>
- npm run build succeeds
</verify>

<done>
- Settings store created with all preference fields
- Settings persist across page refreshes
</done>

---

### Task 2: Create Data Export Function

<files>
- src/utils/export.ts
</files>

<action>
Create utility file `src/utils/export.ts`:
- Import all stores
- `exportAllData()` - Exports goals, tasks, subtasks, sessions, schedule blocks to JSON
- Include metadata: version: "1.0", exportedAt timestamp
- Generate downloadable blob with filename: `goal-os-export-{date}.json`
</action>

<verify>
- npm run build succeeds
</verify>

<done>
- Export produces downloadable JSON file
- JSON contains all app data
</done>

---

### Task 3: Create Data Import Function

<files>
- src/utils/import.ts
</files>

<action>
Create utility file `src/utils/import.ts`:
- `importAllData(file: File)` - Parses JSON
- Validation: check version, required fields (goals, tasks)
- Returns { success: boolean, data?, error? }
- Provide function to load data into stores
</action>

<verify>
- npm run build succeeds
</verify>

<done>
- Import validates JSON structure
- Returns proper error for invalid files
</done>

---

### Task 4: Create Settings Page Component

<files>
- src/components/settings/Settings.tsx
</files>

<action>
Create Settings page component with glassmorphism styling:
- **Pomodoro Settings**: Number inputs for Work, Short Break, Long Break (minutes)
- **Notifications**: Toggle switch for browser notifications
- **Data Management**: 
  - "Export Data" button - downloads JSON
  - "Import Data" button - opens file picker
  - Import success/error feedback
- **About**: Version info, "Clear All Data" button with confirmation
</action>

<verify>
- npm run build succeeds
</verify>

<done>
- Settings page displays all sections
- Export downloads file
- Import accepts JSON and loads data
</done>

---

### Task 5: Add Settings Route and Navigation

<files>
- src/App.tsx
</files>

<action>
- Import Settings component
- Add route: `/settings`
- Add Settings to sidebar navigation (between Planner and All Goals?)
</action>

<verify>
- npm run build succeeds
</verify>

<done>
- Settings accessible at /settings URL
- Sidebar shows Settings link
</done>
